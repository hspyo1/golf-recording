# golf-recording 요구사항 명세서 (spec)

> 이 문서는 Claude Code에게 먼저 읽히는 기준 문서입니다.
> 저장 위치 권장: `docs/spec.md`

---

## 1. 목적

라운딩 결과를 누적 기록하고, 시간이 지나면서 **통계로 볼 수 있게** 만드는 개인용 웹앱.

**성공 기준**
- 라운딩 1건 입력이 3분 이내
- "골프장별 평균타수", "최근 10라운드 추이", "퍼팅 평균" 같은 질문에 즉시 답이 나옴
- 폰과 PC 어디서든 같은 데이터에 접근

---

## 2. 사용 시나리오

| # | 상황 | 기기 | 하는 일 |
|---|---|---|---|
| S1 | 라운딩 중 | 폰 | 홀 끝날 때마다 타수·퍼팅 입력 |
| S2 | 라운딩 후 | PC | 누락분 보정, 비용·메모 정리 |
| S3 | 평상시 | PC | 통계 확인 |
| S4 | 필요 시 | - | 동반자에게 결과 공유 |

---

## 3. 데이터 모델

### 3.1 표 구조

**`courses` — 골프장 마스터**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| name | text | 골프장명 (중복 방지, unique) |
| region | text | 지역 (예: 경기 용인) |
| memo | text | |

**`course_holes` — 홀 정보**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| course_id | FK → courses | |
| nine_name | text | 코스 구분 (OUT / IN / 동 / 서 등) |
| hole_no | int | 1~9 |
| par | int | 기본값 4 |
| handicap | int | 홀 난이도 (선택) |

**`players` — 동반자 명부**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| name | text | |
| display_name | text | 공유 화면 표시용 별칭 (예: "김○○") |
| relation | text | 친구 / 거래처 / 가족 등 |
| memo | text | |

**`rounds` — 라운드 (1건 = 1행)**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| played_on | date | 일자 |
| course_id | FK → courses | |
| nine_combo | text | 플레이한 코스 조합 (예: "OUT-IN") |
| tee_time | time | 선택 |
| weather | text | 맑음/흐림/비/바람 등 |
| round_type | text | 친선 / 접대 / 대회 |
| cost | int | 그린피 등 총비용 (선택) |
| memo | text | 그날의 소회 |

> `total_strokes`, `total_putts`는 **저장하지 말고 hole_scores에서 계산**할 것.
> 중복 저장하면 값이 어긋납니다.

**`hole_scores` — 홀별 상세 (본인 기준)**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| round_id | FK → rounds | |
| course_hole_id | FK → course_holes | |
| strokes | int | 타수 |
| putts | int | 퍼팅 수 |
| fairway_hit | bool/null | 페어웨이 안착. **파3는 null** |
| gir | bool | 그린 적중 (규정타수 내 온그린) |
| penalty | int | 벌타 수, 기본 0 |
| note | text | 선택 |

**`round_players` — 그날의 동반자**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| round_id | FK → rounds | |
| player_id | FK → players | |
| total_score | int | 동반자 총타수 (선택 입력) |

### 3.2 설계 원칙

1. **동반자는 반드시 별도 표로 분리.** 텍스트 한 칸에 "김철수, 이영희"로 넣으면 집계가 불가능해집니다.
2. **골프장·홀 정보는 마스터로 분리.** 매 라운드마다 파를 다시 입력하지 않기 위함.
3. **계산 가능한 값은 저장하지 않는다.** 총타수는 항상 홀별 합계로 도출.
4. 본인 스코어는 `hole_scores`, 동반자 스코어는 `round_players`에 총타수만. (동반자 홀별 기록까지 원하면 Phase 3에서 확장)

---

## 4. 기능 요구사항

### Phase 1 — 로컬에서 완성 (혼자, PC)

| ID | 기능 | 완료 기준 |
|---|---|---|
| F-01 | 골프장 등록/수정 | 홀별 파 18개까지 입력·저장 |
| F-02 | 라운드 신규 등록 | 일자·골프장·코스조합 선택 후 저장 |
| F-03 | 홀별 스코어 입력 | 18홀 타수·퍼팅·페어웨이·GIR 입력, 합계 자동 표시 |
| F-04 | 동반자 등록 및 라운드 연결 | 명부에서 선택 또는 신규 추가 |
| F-05 | 라운드 목록 조회 | 최근순 정렬, 클릭 시 상세 |
| F-06 | 수정/삭제 | 삭제 시 확인 절차 |
| F-07 | CSV 내보내기 | 전체 라운드를 엑셀로 저장 |

### Phase 2 — 폰에서 입력 가능하게

| ID | 기능 | 완료 기준 |
|---|---|---|
| F-08 | 클라우드 DB 전환 | 로컬 SQLite 데이터를 손실 없이 이관 |
| F-09 | 웹 배포 | 폰 브라우저 URL 접속으로 입력 가능 |
| F-10 | 모바일 입력 화면 | 한 화면에 한 홀, 버튼으로 타수 ±, 스크롤 최소화 |
| F-11 | 접근 제한 | 최소한 비밀번호 1개로 잠금 |

### Phase 3 — 통계와 공유

| ID | 기능 | 완료 기준 |
|---|---|---|
| F-12 | 통계 대시보드 | 라운드별 스코어 추이, 골프장별 평균, 파3/4/5별 평균, 평균 퍼팅, 페어웨이 안착률, GIR |
| F-13 | 동반자별 집계 | 특정 동반자와 함께한 라운드 수·평균 스코어 |
| F-14 | 결과 공유 | 라운드 1건을 읽기 전용 링크 또는 이미지로 공유 |

---

## 5. 기술 스택

| 구분 | Phase 1 | Phase 2~3 |
|---|---|---|
| 언어 | Python | 동일 |
| 화면 | Streamlit | 동일 |
| DB | SQLite (파일 1개) | Supabase (PostgreSQL, 무료 등급) |
| 배포 | 로컬 실행 | Streamlit Community Cloud |
| 형상관리 | Git / GitHub | 동일 |

**선정 이유**: 이미 익숙한 Python만으로 화면·DB·배포가 모두 해결되고, 배포하면 폰 브라우저에서도 그대로 열립니다. 새 언어를 배울 필요가 없습니다.

**DB 접근은 반드시 `db.py` 한 파일에 몰아둘 것.** Phase 2에서 SQLite → PostgreSQL로 바꿀 때 그 파일만 고치면 끝나게 하기 위함입니다.

---

## 6. 화면 구성

```
[사이드바]
 ├ 라운드 기록      ← 신규 입력 / 홀별 스코어
 ├ 라운드 목록      ← 조회·수정·삭제
 ├ 통계            ← Phase 3
 └ 설정            ← 골프장 마스터, 동반자 명부
```

**모바일 입력 화면(F-10) 원칙**
- 한 화면 = 한 홀
- 타수는 숫자 키보드가 아니라 `−` `4` `+` 버튼
- "다음 홀" 버튼 하나로 이동, 자동 저장

---

## 7. 비기능 요구사항

- **개인정보**: 동반자 실명이 저장됨. 공유 화면에서는 `display_name`(별칭)만 노출할 것.
- **백업**: 주 1회 CSV 내보내기 또는 DB 파일 백업. Phase 1의 SQLite 파일은 **Git에 커밋하지 말 것**(`.gitignore`에 추가).
- **접속 정보**: DB 접속키는 코드에 직접 쓰지 말고 환경변수 또는 Streamlit secrets로 관리.
- **오프라인**: 골프장 통신이 끊길 수 있음. Phase 2에서 입력 실패 시 재시도 안내 표시.

---

## 8. 이번 범위 밖 (하지 않을 것)

- 회원가입·다중 사용자 로그인 시스템
- 동반자가 직접 입력하는 기능
- 스윙 영상, GPS 거리 측정
- 앱스토어용 네이티브 앱

---

## 9. Claude Code 진행 순서

1. `git init` → GitHub 저장소 생성
2. 이 문서를 `docs/spec.md`로 저장
3. `CLAUDE.md` 작성 — "이 프로젝트는 docs/spec.md를 기준으로 한다. Phase 1 범위만 구현한다." 명시
4. **DB 스키마부터 구현하고 커밋** (F-01 이전에)
5. 기능 ID 하나씩 = 커밋 하나씩. 한 번에 여러 기능을 시키지 말 것
6. Phase 1 완료 후 실제 라운딩 2~3회 기록해보고, 불편한 점을 반영한 뒤 Phase 2 착수

---

## 부록: 용어

- **PK / FK**: 각 행의 고유번호 / 다른 표를 가리키는 연결번호. 회계로 치면 전표번호와 계정코드 관계.
- **GIR (Green in Regulation)**: 파4에서 2타 이내, 파5에서 3타 이내 온그린 성공 여부.
- **스키마(schema)**: 어떤 표에 어떤 항목을 담을지 정한 설계도.
- **마스터 테이블**: 반복 사용되는 기준 정보를 모아둔 표. 거래처원장과 같은 개념.
- **Supabase**: 무료로 쓸 수 있는 클라우드 데이터베이스 서비스.
- **Streamlit Community Cloud**: Streamlit 앱을 무료로 인터넷에 올려주는 서비스.
