# golf-recording 요구사항 명세서 (spec)

> 이 문서는 Claude Code에게 먼저 읽히는 기준 문서입니다.
> 저장 위치 권장: `docs/spec.md`

---

## 0. 스택 변경 이력

- **2026-08-24**: Streamlit + SQLite → 정적 HTML + localStorage로 전환. 이유: 폰에서 설치 없이 열리고 데이터가 기기 밖으로 나가지 않음. 배포는 GitHub Pages.
- **2026-08-24**: F-09(GitHub Pages 배포) 완료 — `https://hspyo1.github.io/golf-recording/`. F-12(오프라인 캐시, 서비스워커) 구현 완료.
- **2026-08-24**: F-10(모바일 입력 화면) 구현 완료 — 홀별 스코어 입력에 "한 화면 = 한 홀" 카드 뷰(+/− 스테퍼, 다음 홀 자동저장) 추가. 뷰포트 메타 태그 추가로 폰에서 확대 없이 정상 크기로 보이도록 수정. 전체 표 보기(PC 보정용)는 토글로 유지.
- **2026-08-24**: F-01 확장 — "자주 가는 골프장"에 사용자가 직접 골프장명·지역·코스구성을 추가할 수 있게 함(`customSeedCourses`, localStorage에 저장). 골프장 등록 폼도 수정 모드일 때 버튼 문구가 "수정 저장"으로 바뀌어, 다른 골프장을 새로 등록하려다 기존 골프장을 실수로 덮어쓰는 사고를 방지.

---

## 1. 목적

라운딩 결과를 누적 기록하고, 시간이 지나면서 **통계로 볼 수 있게** 만드는 개인용 웹앱.

**성공 기준**
- 라운딩 1건 입력이 3분 이내
- "골프장별 평균타수", "최근 10라운드 추이", "퍼팅 평균" 같은 질문에 즉시 답이 나옴
- 폰과 PC 어디서든 같은 데이터에 접근

---

## 2. 사용 시나리오

| # | 상황 | 기기 | 하는 일 |
|---|---|---|---|
| S1 | 라운딩 중 | 폰 | 홀 끝날 때마다 타수·퍼팅 입력 |
| S2 | 라운딩 후 | PC | 누락분 보정, 비용·메모 정리 |
| S3 | 평상시 | PC | 통계 확인 |

---

## 3. 데이터 모델

### 3.1 표 구조

**`courses` — 골프장 마스터**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| name | text | 골프장명 (통째로 하나의 이름, 예: "라비에벨 올드코스") |
| region | text | 지역 (예: 경기 용인) |

> 골프장은 전/후반 구분 없이 코스 전체를 등록한다. 코스구분(`course_holes.nine_name`)은 홀마다 직접 입력하거나(예: OUT/IN, 또는 더크로스비처럼 아리아/빌리/샬롯) 자주 가는 골프장 목록에서 불러와 채운다. 라운드 등록 시에만 그중 전반/후반으로 쓸 코스 두 개를 선택한다.

**`course_holes` — 홀 정보**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| course_id | FK → courses | |
| nine_name | text | 코스 구분 (OUT / IN / 동 / 서 등) |
| hole_no | int | 1~9 |
| par | int | 기본값 4 |
| handicap | int | 홀 난이도 (선택) |

**`rounds` — 라운드 (1건 = 1행)**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| played_on | date | 일자 |
| course_id | FK → courses | |
| nine_combo | text | 플레이한 코스 조합 (예: "OUT-IN") |
| tee_time | time | 선택 |
| weather | text | 맑음/흐림/비/바람 등 |
| round_type | text | 친선 / 접대 / 대회 |
| cost | int | 그린피 등 총비용 (선택) |
| memo | text | 그날의 소회 |

> `total_strokes`, `total_putts`는 **저장하지 말고 hole_scores에서 계산**할 것.
> 중복 저장하면 값이 어긋납니다.

**`hole_scores` — 홀별 상세 (본인 기준)**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| round_id | FK → rounds | |
| course_hole_id | FK → course_holes | |
| strokes | int | 타수 |
| putts | int | 퍼팅 수 |
| fairway_hit | bool/null | 페어웨이 안착. **파3는 null** |
| gir | bool | 그린 적중 (규정타수 내 온그린) |
| note | text | 선택 |

### 3.2 설계 원칙

1. **골프장·홀 정보는 마스터로 분리.** 매 라운드마다 파를 다시 입력하지 않기 위함.
2. **계산 가능한 값은 저장하지 않는다.** 총타수는 항상 홀별 합계로 도출.

---

## 4. 기능 요구사항

### Phase 1 — 로컬에서 완성 (혼자, PC)

| ID | 기능 | 완료 기준 |
|---|---|---|
| F-01 | 골프장 등록/수정 | 홀별 파 18개까지 입력·저장 |
| F-02 | 라운드 신규 등록 | 일자·골프장·코스조합 선택 후 저장 |
| F-03 | 홀별 스코어 입력 | 18홀 타수·퍼팅·페어웨이·GIR 입력, 합계 자동 표시 |
| F-05 | 라운드 목록 조회 | 최근순 정렬, 클릭 시 상세 |
| F-06 | 수정/삭제 | 삭제 시 확인 절차 |
| F-07 | CSV 내보내기 | 전체 라운드를 엑셀로 저장 |

### Phase 2 — 폰에서 입력 가능하게

| ID | 기능 | 완료 기준 |
|---|---|---|
| F-08 | 동반자와 데이터 공유 (방식 미정) | 로컬 데이터를 손실 없이 이관 |
| F-09 | 웹 배포 | 폰 브라우저 URL 접속으로 입력 가능 |
| F-10 | 모바일 입력 화면 | 한 화면에 한 홀, 버튼으로 타수 ±, 스크롤 최소화 |
| F-11 | 접근 제한 | 최소한 비밀번호 1개로 잠금 |
| F-12 | 오프라인 캐시 | 서비스워커로 앱 파일을 캐시해, 통신이 끊겨도 앱이 열리고 입력·저장이 됨 |

### Phase 3 — 통계와 공유

| ID | 기능 | 완료 기준 |
|---|---|---|
| F-13 | 통계 대시보드 | 라운드별 스코어 추이, 골프장별 평균, 파3/4/5별 평균, 평균 퍼팅, 페어웨이 안착률, GIR |
| F-15 | 결과 공유 | 라운드 1건을 읽기 전용 링크 또는 이미지로 공유 |

---

## 5. 기술 스택

| 구분 | Phase 1 | Phase 2~3 |
|---|---|---|
| 언어 | JavaScript (브라우저 내장, 별도 설치 불필요) | 동일 |
| 화면 | HTML/CSS/JS (index.html 단일 파일) | 동일 |
| DB | localStorage (브라우저 내장 저장소) | 미정 (공유 단계에서 결정) |
| 배포 | 로컬 실행 | GitHub Pages |
| 형상관리 | Git / GitHub | 동일 |

**선정 이유**: 설치나 서버 없이 아이폰 홈 화면에서 바로 열리고, 입력한 데이터가 기기 밖으로 나가지 않는다. 파일이 하나라 배포와 수정이 단순하다.

**저장·불러오기 로직은 storage 모듈 한 곳에만 둘 것.** 나중에 저장 위치를 바꿀 때 그 부분만 고치면 되게 하기 위함이다.

---

## 6. 화면 구성

아이폰 세로 화면(폭 390px) 기준, 별도 페이지 전환 없이 한 화면(index.html) 안에서 아래 섹션들을 위아래로 오가며 사용한다.

```
[한 화면 안의 섹션들]
 ├ 골프장 관리      ← 골프장 마스터, 홀 정보
 ├ 라운드 신규 등록
 ├ 홀별 스코어 입력
 ├ 라운드 목록      ← 조회·수정·삭제, CSV 내보내기
 └ 통계            ← Phase 3
```

**모바일 입력 화면(F-10) 원칙**
- 한 화면 = 한 홀
- 타수는 숫자 키보드가 아니라 `−` `4` `+` 버튼
- "다음 홀" 버튼 하나로 이동, 자동 저장

---

## 7. 비기능 요구사항

- **백업**: 백업은 CSV 내보내기로 한다. localStorage는 브라우저 데이터 삭제 시 사라지므로 주기적 내보내기가 필요하다.
- **접속 정보**: [보류] 공유 기능 도입 시 재검토.
- **오프라인**: 골프장 통신이 끊겨도 앱이 열리고 입력·저장이 되어야 한다. 서비스워커로 앱 파일을 캐시한다. (F-12)

---

## 8. 이번 범위 밖 (하지 않을 것)

- 회원가입·다중 사용자 로그인 시스템
- 동반자가 직접 입력하는 기능
- 스윙 영상, GPS 거리 측정
- 앱스토어용 네이티브 앱

---

## 9. Claude Code 진행 순서

1. `git init` → GitHub 저장소 생성
2. 이 문서를 `docs/spec.md`로 저장
3. `CLAUDE.md` 작성 — "이 프로젝트는 docs/spec.md를 기준으로 한다. Phase 1 범위만 구현한다." 명시
4. **DB 스키마부터 구현하고 커밋** (F-01 이전에)
5. 기능 ID 하나씩 = 커밋 하나씩. 한 번에 여러 기능을 시키지 말 것
6. Phase 1 완료 후 실제 라운딩 2~3회 기록해보고, 불편한 점을 반영한 뒤 Phase 2 착수

---

## 부록: 용어

- **PK / FK**: 각 행의 고유번호 / 다른 표를 가리키는 연결번호. 회계로 치면 전표번호와 계정코드 관계.
- **GIR (Green in Regulation)**: 파4에서 2타 이내, 파5에서 3타 이내 온그린 성공 여부.
- **스키마(schema)**: 어떤 표에 어떤 항목을 담을지 정한 설계도.
- **마스터 테이블**: 반복 사용되는 기준 정보를 모아둔 표. 거래처원장과 같은 개념.
- **Supabase**: 무료로 쓸 수 있는 클라우드 데이터베이스 서비스. (공유 단계 후보)
- **GitHub Pages**: 깃허브에 올린 파일을 무료로 웹사이트 주소로 띄워주는 서비스.
