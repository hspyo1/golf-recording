# CLAUDE.md

이 파일은 Claude Code가 매 세션 시작 시 자동으로 읽는 프로젝트 규칙 문서입니다.

---

## 프로젝트 개요

**golf-recording** — 개인용 골프 라운딩 기록 웹앱.
라운딩 결과(일자·골프장·홀별 스코어)를 누적 저장하고 통계로 확인한다.

## 기준 문서

- **요구사항(무엇을 만들지)의 기준은 `golf-recording-spec.md`이다.**
- **기술 스택·구조(어떻게 만들지)의 기준은 이 문서(CLAUDE.md)이다.** spec.md 5장의 Python/Streamlit/SQLite 기술 스택은 더 이상 유효하지 않다 — 아래 "기술 스택"을 따른다.
- spec.md 3장(데이터 모델)의 표 구조는 개념적으로 유지하되, 실제로는 SQL 표가 아니라 JSON 안의 배열로 구현한다.
- spec.md에 없는 기능은 구현하지 않는다. 필요하다고 판단되면 **먼저 물어본다.**

## 현재 작업 범위

**Phase 1**(F-01 ~ F-07)은 완료. 이어서 **Phase 2 중 배포·동기화 관련 항목**(F-09 GitHub Pages 배포, F-12 오프라인 캐시)을 완료했고,
**PC↔폰 기기 간 데이터 동기화**를 Supabase로 구현 중이다 (아래 "기술 스택" 참고).
그 외 Phase 2·3 기능(통계 대시보드, 공유 링크 등)은 지시가 있을 때까지 착수하지 않는다.

---

## 기술 스택 (변경 금지)

- **순수 HTML + CSS + JavaScript (바닐라).** 빌드 도구 없음. 프레임워크 없음.
- **단일 파일 `index.html`** — 브라우저에서 파일을 직접 열어 실행하거나, GitHub Pages로 접속해 실행한다.
- **데이터 저장: 브라우저 `localStorage`를 기본(로컬 캐시/오프라인 폴백)으로 하고, Supabase를 기기 간 동기화 저장소로 사용한다.**
  - 항목마다 따로 저장하지 않는다. 앱 전체 데이터를 객체 하나로 묶어 **JSON 문자열로, 키 이름 하나**에 저장한다. (예: `localStorage["golfRecordingData"]`)
  - 페이지를 열 때 그 키에서 불러와 화면을 복원한다.
  - **Supabase 연동은 예외적으로 허용된 서버/백엔드 의존이다.** 무료 요금제 한도 내에서만 쓰고, API 키 등 접속 정보는 코드에 하드코딩하지 않는다(코딩 규칙 6번). Supabase 외 다른 백엔드·서버·클라우드 서비스는 여전히 금지.
- **CSV 내보내기 버튼**을 두어 백업할 수 있게 한다. (F-07)
- **오프라인 캐시(F-12)**: 서비스워커(`sw.js`)로 `index.html`을 캐시해, 통신이 끊겨도 앱이 열리고 로컬 저장은 계속된다. (Supabase 동기화는 온라인일 때만 동작)

Supabase 외 새 라이브러리·CDN 스크립트가 필요하면 **설치(추가) 전에 이유를 설명하고 승인을 받는다.**

## 폴더 구조

```
golf-recording/
├── CLAUDE.md
├── golf-recording-spec.md     ← 요구사항 기준 문서 (docs/ 로 옮기지 않음)
├── README.md
├── index.html                 ← 화면·로직(HTML/CSS/JS)이 이 한 파일 안에 있다
├── sw.js                      ← 오프라인 캐시용 서비스워커 (F-12)
├── manifest.json              ← 홈 화면 추가 시 앱 이름·아이콘·테마색 (F-09/F-10)
└── icons/                     ← 홈 화면 아이콘 PNG (apple-touch-icon, icon-192, icon-512, favicon-32)
```

---

## 코딩 규칙

1. **데이터 저장 접근(localStorage read/write)은 하나의 JS 모듈(함수 그룹)에만 작성한다.**
   화면을 그리는 코드에서 `localStorage`를 직접 건드리지 않는다.
   → 나중에 저장 방식을 바꾸더라도 이 함수들만 고치면 되게 하기 위함이다.

2. **계산 가능한 값은 저장하지 않는다.**
   총타수·총퍼팅은 `holeScores` 배열에서 매번 합산해서 화면에 보여준다. 별도 필드로 저장하지 않는다.

3. **화면 문구(사용자에게 보이는 텍스트)는 영어로 작성한다.** 골프장명·지역명 등 고유명사는 원문 그대로 둔다. 주석은 한국어로 작성한다(사용자와 한국어로 협업하기 때문). 변수명·함수명은 영어.

4. **파3 홀의 `fairwayHit`은 `null`로 저장한다.** `0`이나 `false`가 아니다.

5. 삭제 기능에는 반드시 확인 절차(예: `confirm()` 또는 확인 버튼)를 둔다.

6. 비밀번호·접속키를 코드에 쓰지 않는다. **단, Supabase의 URL과 anon(public) key는 예외다** — Row Level Security로 보호되는 공개용 키라 클라이언트 코드에 그대로 두어도 된다. service_role key 등 비공개 키는 절대 코드에 넣지 않는다.

7. **표 구조는 spec.md 3.1을 따르되 JS 배열로 구현한다.**
   `rounds`, `holeScores` — 각 배열의 각 항목은 spec.md의 컬럼과 동일한 필드를 가지며, 정수 `id`를 직접 채번해서 부여한다(다음 id 카운터를 데이터 객체에 함께 저장). 골프장/코스명·홀별 par는 마스터 테이블 없이 `rounds`·`holeScores`에 직접 입력한 값을 저장한다(2026-08-25 F-01 폐기, spec.md 이력 참고).

---

## 작업 방식

1. **데이터 모델(스토리지 스키마)과 저장/조회 함수들을 가장 먼저 완성하고 커밋한다.** 화면은 그 다음이다.
2. **기능 ID 하나 = 커밋 하나.** 여러 기능을 한 번에 구현하지 않는다.
3. 코드를 작성하기 전에 **무엇을 만들지 한 줄로 요약해서 먼저 말한다.**
4. 기존 파일을 크게 갈아엎기 전에는 반드시 확인을 구한다.
5. 각 기능 완료 후 브라우저에서 열어 오류가 없는지 확인한 뒤 커밋한다.

## 커밋 메시지 규칙

```
[F-03] 홀별 스코어 입력 화면 구현
[fix] 파3 페어웨이 값이 0으로 저장되던 문제 수정
[chore] .gitignore 정리
```

## .gitignore 필수 항목

```
.DS_Store
```

(서버·DB 파일이 없으므로 별도 제외 항목은 없음)

---

## 실행 방법

`index.html` 파일을 브라우저에서 더블클릭해서 열거나, GitHub Pages 주소로 접속한다. (별도 서버 실행 불필요)
Supabase 동기화는 온라인일 때만 동작하며, 오프라인일 때는 localStorage만 쓰고 이후 온라인이 되면 동기화한다.

---

## 하지 말 것

- spec.md에 없는 기능 임의 추가 (로그인, 영상, GPS 등)
- Phase 2·3 기능 중 지시 없는 것 미리 작성 (통계 대시보드, 공유 링크 등)
- Supabase 외의 서버·백엔드·빌드 도구 도입
- 사용자 확인 없이 기존 localStorage/Supabase 데이터 삭제·마이그레이션 실행
